This is a small project that simulates (empty) windows on multiple workspaces with a dock (in progress).
It only uses Javascript, HTML and CSS and requires node.js and express.
To run it, launch

```
node server.js
```

and open a browser at http://localhost:8888/
