This is a small project that simulates windows on multiple workspaces with a dock.
It only uses Javascript, HTML and CSS and requires node.js.
To run it, launch

```
node server.js
```

and open a browser at http://localhost:8888/